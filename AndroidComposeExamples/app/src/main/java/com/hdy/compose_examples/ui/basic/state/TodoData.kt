package com.hdy.compose_examples.ui.basic.state

object TodoData {
    val data = listOf(
        TodoItem(task = "learn english", TodoIcon.Expand),
        TodoItem(task = "learn english", TodoIcon.Done),
        TodoItem(task = "learn english", TodoIcon.Event),
        TodoItem(task = "learn english", TodoIcon.Privacy),
        TodoItem(task = "learn english", TodoIcon.Restore),
    )
}
